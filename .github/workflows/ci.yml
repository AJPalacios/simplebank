name: CI-test

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    test:
        name: Test app
        runs-on: ubuntu-latest

        services:
          postgres:
            image: postgres
            env:
              POSTGRES_USER: myuser
              POSTGRES_PASSWORD: mysecretpassword
              POSTGRES_DB: simple_bank
            options: >-
              --health-cmd pg_isready
              --health-interval 10s
              --health-timeout 5s
              --health-retries 5

        steps:
          - name: Set up Go
            uses: actions/setup-go@v4
            with:
              go-version: '1.24.3'
            id: go

          - name: Check out code
            uses: actions/checkout@v3

          - name: Install Golang Migrate
            run: |  
              curl -L https://github.com/golang-migrate/migrate/releases/download/v4.19.0/migrate-4.19.0.zip | tar xvz
              sudo mv migrate.linux-amd64 /usr/bin/migrate
              which migrate
          - name: Run migrations
            run: make migrateup

          - name: Test
            run: make test